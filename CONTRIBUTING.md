# Contributing to CoffeeLog

CoffeeLog에 관심을 가져주셔서 감사합니다. 버그 제보, 기능 제안, 문서 개선, 작은 문구 수정까지 모두 환영합니다.

## 시작하기

```bash
npm install
npm run typecheck
```

앱을 실행하려면 다음 명령을 사용합니다.

```bash
npm start
```

웹에서 빠르게 확인하려면 다음 명령을 사용합니다.

```bash
npm run web
```

## 작업 방식

1. 이슈가 있으면 먼저 관련 이슈를 확인합니다.
2. 변경 범위를 작게 유지하고, 한 PR에는 하나의 목적만 담습니다.
3. UI 문구는 앱 안의 기존 톤을 따라 한국어 사용자를 기준으로 작성합니다.
4. 저장소, 백업, 내보내기, Android 빌드 관련 변경은 영향 범위를 PR에 적어주세요.
5. 제출 전 `npm run typecheck`를 실행합니다.

## Pull Request 체크리스트

- 변경 이유와 사용자에게 보이는 차이를 설명했나요?
- `npm run typecheck`를 통과했나요?
- 화면 변경이 있다면 스크린샷이나 짧은 설명을 남겼나요?
- 민감 정보, 로컬 빌드 산출물, 로그 파일이 포함되지 않았나요?

## 이슈 작성 팁

버그 제보에는 재현 순서, 기대 동작, 실제 동작, 실행 환경을 적어주세요. 기능 제안에는 어떤 커피 기록 흐름이 더 편해지는지 함께 적어주시면 좋습니다.
